# Core dependencies for Seldon Core model serving
# These are needed to run our custom inference server

# Transformers for our DistilBERT model
transformers==4.30.0

# PyTorch for model inference (CPU version for smaller images)
torch==1.13.1

# NumPy for array operations (Seldon expects numpy arrays)
numpy>=1.24.0

# Additional transformers dependencies
tokenizers==0.13.3
